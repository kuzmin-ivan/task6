import React, { Component } from 'react';
import styles from './LetterSection.module.css';

export class LetterSection extends Component {
  public render() {
    return <section className={styles.lettersSection} />;
  }
}
